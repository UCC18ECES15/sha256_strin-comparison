#include <stdio.h>
#include <stdlib.h>
#include <assert.h>
#include "sha.h"
 uint8_t hash[32];
void hash_to_string(char string[64], const uint8_t hash[32])
{
	size_t i;
	for (i = 0; i < 32; i++) {
		string += sprintf(string, "%02x", hash[i]);

	}
}
int main()
{

char hash_string[64];

    char input[2];
    for(int i=0;i<2;i++)
    {
        printf("enter the input:\n");
        scanf("%s",input);
        printf("\n");
        printf("entered input is:%s \n\n",input);
        calc_sha_256(hash,input,strlen(input));
        hash_to_string(hash_string,hash);
        printf("The entered input's sha key is: \n\n");
        printf("%s \n\n",hash_string);
    }
    for(int i=0;i<2;i++)
    {
        sprintf("%s",hash[i]);
    }

    return 0;
}
